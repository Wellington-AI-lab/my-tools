{
  "name": "news-api-worker",
  "version": "1.0.0",
  "description": "新闻聚合 API - Cloudflare Worker + D1",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240528.0",
    "@vitest/coverage-v8": "^1.6.0",
    "miniflare": "^3.20240807.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.0.0"
  }
}
