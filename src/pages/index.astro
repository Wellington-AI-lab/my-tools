---
import AppLayout from '@/layouts/AppLayout.astro';
import { MODULES } from '@/config/modules';
import ModuleCard from '@/components/ModuleCard.astro';
---

<AppLayout title="My Tools">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-onekey-text-primary font-english">My Tools</h1>
    <p class="mt-2 text-sm text-onekey-text-secondary">
      模块彼此独立，统一 UI 规范，通过 API 共享"标的池/标签"等平台数据。
    </p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    {MODULES.map((m) => <ModuleCard module={m} />)}
  </div>
</AppLayout>


